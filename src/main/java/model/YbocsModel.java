package model;

public class YbocsModel {
/**
 * import java.util.Scanner;
 
public class MyClass {
    public static void main(String args[]) {
       
    int[] responses = {0,1,2,3,4,5};
      
      String q1 = "how bad does your back hurt?";
      String q2 = "how bad does your head hurt?";
      Question [] questions = {
          new Question(q1, responses),
           new Question(q2, responses)
      };
      takeTest(questions);
     }
     
     public static void takeTest(Question[] questions){
         int score = 0;
         Scanner keyboad = new Scanner(System.in);
         for(int i=0; i<questions.length; i++){
             System.out.print(questions[i].prompt);
             int answer = keyboad.nextInt();
            // for (int element: responses){
                 //if (element == answer){
                     score = score + answer;
               //  }else{
                // System.out.println("NON");
             //}
            // }
              
             //if (answer < responses)){
              //   score= score+ answer;
             //} 
         }
         System.out.print(score);
         
     }
}

class Question{
    String prompt;
    int[] answer;
    
    public Question(String prompt, int[] answer){
        this.prompt = prompt;
        this.answer = answer;
    }
}

 */
}
